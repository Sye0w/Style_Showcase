import { RevealonScrollDirective } from './revealon-scroll.directive';

describe('RevealonScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealonScrollDirective();
    expect(directive).toBeTruthy();
  });
});
